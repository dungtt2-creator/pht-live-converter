/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // static export thuần — deploy Vercel/Vercel static hoặc GitHub Pages đều được
  reactStrictMode: true,
};

export default nextConfig;